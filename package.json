{
  "scripts": {
    "start-server": "cd server && npm run start:dev",
    "start-watch": "cd client && yarn watch",
    "start-client": "cd client && yarn dev --host",
    "start": "npm-run-all -p start-server start-watch start-client"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}